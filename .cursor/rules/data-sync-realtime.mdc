---
description: 代码变更时必须保持 UI、内存状态、localStorage 与跨组件事件同步，确保数据实时一致
alwaysApply: true
---

# 数据同步与实时更新

本项目为纯前端架构，无服务端/WebSocket。「实时」指：改数据后 UI 立即反映，内存与 localStorage 一致，相关组件通过事件联动。

## 三层 reactive 状态必须联动

- `src/state/appState.js` — 当前题集、首页 txt、站点配置
- `src/state/questionBankState.js` — 本地/远程题库列表
- `src/state/questionProgressState.js` — 当前会话答题统计（FAB、练习页按钮）

改任一层的数据来源或写入路径时，检查其余两层是否需要同步更新。

## localStorage 三键

定义于 `src/services/browserStorage.js`：

- `_txt_local_banks` — 本地题库
- `_txt_remote_cache` — 远程缓存
- `_txt_practice_progress` — 练习进度（`schemaVersion: 1`）

写入 localStorage 后**必须**调用 `notifyStorageChanged(kind)`（`src/services/appStorageSync.js`）。

## 新增或修改读写路径时的检查项

- 是否需要订阅 `subscribeStorageChanged` / 监听 `txt-storage-changed`（参考 `AppTopNav.vue`）
- 是否在页面隐藏/卸载时 flush 持久化（参考 `AppQuestion.vue` 400ms 防抖 + flush）
- 恢复进度是否走 `applyProgressToQuestions`（`src/services/practiceProgress.js`）

## 答题 UI 实时性

- 答题时使用 `src/models/question/progress.ts` 的 `notifySlotChanged` 更新 `questionProgressState`
- **禁止**仅写 localStorage 而不更新内存层统计

## 已知待完善点

改同步相关代码时应一并评估：

- `initAppStorageSync()` 尚未在应用入口初始化（跨标签页 `storage` 事件）
- `PracticeProgressView.vue` 未订阅存储变更事件，停留该页时其他页面的保存不会自动刷新列表

## 完成改动后自检

- [ ] 改数据后 UI 立即反映，无需刷新页面
- [ ] localStorage 与内存状态一致
- [ ] 导航角标、进度列表等相关 UI 会随事件更新
