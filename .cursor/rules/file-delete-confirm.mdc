---
description: 涉及文件删除时必须先向用户二次确认，获准后再执行
alwaysApply: true
---

# 文件删除二次确认

凡会**删除、移除或清空**用户工作区内文件/目录的操作，必须先二次确认，**禁止**在未获用户明确同意时直接执行。

## 适用操作

- 删除工具（如 Delete）
- Shell：`rm`、`rmdir`、`unlink`、`git rm`、清空目录内容等
- 用「先删后写」或覆盖方式导致原文件不可恢复的操作
- 批量删除、移动到回收站以外的永久删除

## 确认流程

1. **先说明**：列出将删除的路径（绝对或相对均可）、原因、是否可恢复
2. **等待确认**：明确请用户回复同意（如「确认删除」「可以删」）
3. **获准后再删**：仅删除用户已确认的路径；未点名的路径不得顺带删除
4. **用户拒绝或未回复**：停止删除，不改文件

## 示例

```
❌ BAD：直接 rm -rf src/components/OldWidget.vue

✅ GOOD：先告知
「将删除 src/components/OldWidget.vue（该组件已无引用）。确认删除请回复：确认删除」
→ 用户确认后再执行删除
```

## 例外

仅当用户在**同一条消息**中已明确点名路径并要求删除（如「删除 `foo.ts`」）时，可直接执行该路径，无需再问一遍。若范围扩大、路径模糊或涉及批量/目录，仍须二次确认。
