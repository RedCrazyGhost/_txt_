---
description: 改代码前必须离开主分支，按变动类型新建或切换到对应功能分支
alwaysApply: true
---

# 改动前先开分支

凡会**修改工作区代码/配置**的任务，**禁止**直接在 `main` / `master` 上改文件或 commit。动手前先确认并切到对应功能分支。

## 流程（改文件之前）

1. `git branch --show-current` 与 `git status -sb` 确认当前分支与工作区状态
2. 若已在与本次任务相关的功能分支上 → 继续使用，勿重复建枝
3. 若在 `main` / `master`（或用户指定的默认发布分支）→ **先建分支再改代码**：
   ```bash
   git checkout -b <type>/<short-description>
   ```
4. 未提交改动已在主分支时：用 `git checkout -b ...` 带走工作区改动即可，**不要**先 commit 到主分支再迁走

## 分支命名

按变动性质选前缀，描述用短横线英文或拼音，简洁可扫：

| 前缀 | 适用 |
|------|------|
| `feat/` | 新功能 |
| `fix/` | Bug 修复 |
| `refactor/` | 重构（行为不变） |
| `chore/` | 工具、规则、依赖、杂项 |
| `docs/` | 仅文档 |

```
✅ feat/practice-timer
✅ fix/progress-fab-sync
✅ chore/cursor-git-branch-rule

❌ main 上直接改
❌ 无意义名：temp、test、update、wip
```

## 例外（可留在主分支）

仅当满足其一：

- 用户**明确**要求在主分支上改（如「直接改 main」）
- **纯只读**：查代码、答疑、不改任何文件
- 仅按既有规则做 **push 前版本 bump** 且用户已要求 push（仍优先在发布流程约定的分支上操作）

## 与本地保存规则的关系

开好功能分支后，再按「变更后本地 Git 保存」规则在**当前功能分支**上 commit；未经用户明确要求，仍禁止 `git push`。
