---
description: push 到远端前网站版本 patch +1，且 package.json 与 appState 保持一致
alwaysApply: true
---

# Push 前版本递增

每次 push 到远端前，网站版本必须 semver patch +1（如 `2.0.1` → `2.0.2`）。

## 仅 bump 网站版本

**禁止**误改以下与网站发布版本无关的字段：

- QuestionJSON 题集版本（`0.0.2` / `0.0.3`，见 `src/models/question/normalize.ts`）
- 练习进度 `SCHEMA_VERSION`（`src/services/practiceProgress.js`）

## 必须同步更新的文件

1. `package.json` — `version`
2. `package-lock.json` — 顶层 `version`
3. `src/state/appState.js` — `webSiteConfig.appVersion`

三处版本号必须完全一致。

## Push 前流程

1. 运行 `npm run version:patch`
2. 确认 diff 仅含版本相关文件
3. commit 版本变更
4. push 到远端

pre-push hook 会强制执行此流程：若本地版本不大于远端，会自动 bump 并中止 push，提示先 commit。

## UI 展示

`AppBottomNav.vue` 读取 `appState.webSiteConfig.appVersion`，通常无需改组件。

Agent 协助 push 时，应先完成版本 bump 与 commit，再执行 push。
