---
description: 涉及题目数据模型变更时，必须先告知用户并谨慎评估对进度、题库与判题的影响
globs: src/models/question/**,QuestionJSON/**,src/services/practiceProgress.js,src/services/questionBank.js,src/utils/questions.ts
alwaysApply: false
---

# 题目数据模型变更须谨慎

涉及题目结构、题型、归一化、判题或 QuestionJSON 数据时，**动手前必须先向用户说明**变更范围与影响，未获确认前不得实施破坏性改动。

## 动手前必须告知用户

- 变更范围（字段、题型、QuestionJSON 版本）
- 对 localStorage 旧进度、旧题库导入的影响
- 是否需要升级 `practiceProgress` 的 `SCHEMA_VERSION` 并编写迁移

## 核心契约（不可轻易破坏）

- `answers` 槽位与 `results` 按**题目索引**一一对应（`src/models/question/progress.ts`、`src/services/practiceProgress.js`）
- 增删或重排题目会导致 `questionCountMismatch`，旧进度失效
- 0.0.2（`texts`/`answers`）与 0.0.3（`stem`/`options`/`questionType`）双轨并存，版本由 `resolveQuestionBankVersion()` 推断，不依赖 JSON 文件中的 `version` 字段

## 改动 checklist（逐项确认）

- [ ] 更新 `types.ts`、`normalize.ts` 及对应测试
- [ ] 检查 `judgeAnswerTrue`、`QuestionCard.vue`、各 `*QuestionBody.vue`
- [ ] 评估 `practiceProgress` 是否需 schema 升级与迁移
- [ ] 运行 `npm test` 中与 normalize、questions、practiceProgress 相关用例

## 禁止

- 未获用户确认前直接批量修改 `QuestionJSON/` 数据
- 未评估影响前破坏性变更 `answers`/`results` 语义或槽位模型
